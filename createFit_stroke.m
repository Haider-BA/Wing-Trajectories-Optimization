function [fitresult, gof] = createFit_stroke(t, x1)
%CREATEFIT1(T,X1)
%  Create a fit.
%
%  Data for 'untitled fit 1' fit:
%      X Input : t
%      Y Output: x1
%  Output:
%      fitresult : a fit object representing the fit.
%      gof : structure with goodness-of fit info.
%
%  See also FIT, CFIT, SFIT.

%  Auto-generated by MATLAB on 02-Mar-2013 00:30:21


%% Fit: 'untitled fit 1'.
[xData, yData] = prepareCurveData( t, x1 );

% Set up fittype and options.
ft = fittype( 'fourier3' );
opts = fitoptions( ft );
opts.Display = 'Off';
opts.Lower = [-Inf -Inf -Inf -Inf -Inf -Inf -Inf -Inf];
opts.StartPoint = [0 0 0 0 0 0 0 2*pi];
opts.Upper = [Inf Inf Inf Inf Inf Inf Inf Inf];

% Fit model to data.
[fitresult, gof] = fit( xData, yData, ft, opts );

% % Plot fit with data.
% figure( 'Name', 'stroke' );
% h = plot( fitresult, xData, yData );
% legend( h, 'x1 vs. t', 'stroke', 'Location', 'NorthEast' );
% % Label axes
% xlabel( 't' );
% ylabel( 'x1' );
% grid on


